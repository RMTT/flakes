apiVersion: v2
name: nextcloud
description: Nextcloud deployment
type: application
version: 0.1.0

dependencies:
  - name: nextcloud
    version: "9.1.2"
    repository: "https://nextcloud.github.io/helm/"
